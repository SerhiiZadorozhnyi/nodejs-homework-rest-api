const { joiSchema } = require('./contact')
// const { joiSchemaStatusContact } = require('./contact')
const { updateFavoriteJoiSchema } = require('./contact')
const { Contact } = require('./contact')

module.exports = {
  joiSchema,
  // joiSchemaStatusContact,
  updateFavoriteJoiSchema,
  Contact
}
